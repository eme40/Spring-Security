spring:
  datasource:
    url: jdbc:mysql://localhost:3306/springSecurityDb
    username: root
    password: Eric7713@
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database: mysql

  mail:
    host: smtp.gmail.com
    port: 587
    username: anayoeric40@gmail.com
    password: ${EMAIL_PASSWORD}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
